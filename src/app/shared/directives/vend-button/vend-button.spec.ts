import { VendButton } from './vend-button';

describe('VendButton', () => {
  it('should create an instance', () => {
    const directive = new VendButton();
    expect(directive).toBeTruthy();
  });
});
